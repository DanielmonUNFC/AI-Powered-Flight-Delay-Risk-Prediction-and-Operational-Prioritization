# Dashboard assets

Static media used by the Streamlit dashboard.

## Team photos

Place headshots in `team/` using the filenames defined in `config/project_info.py`.

## Icons

Panel and tech icons render from `utils/dashboard_icons.py`.
Icon IDs are defined in `config/panel_icons.py`.
