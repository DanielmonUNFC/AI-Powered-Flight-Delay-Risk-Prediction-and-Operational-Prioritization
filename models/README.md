# Models

This directory stores only the approved models used by the application.

Temporary models, checkpoints, and experiment artifacts should not be committed to the repository.

Model training history and experiment tracking are managed through MLflow.